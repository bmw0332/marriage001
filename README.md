# 結婚相談所LP 納品物ドキュメント

`index.html` 1ファイル（CSS・JS内包）＋ `images/` のみで動く構成です。
GitHub Pages にそのまま置けば公開できます。ビルド作業は不要です。

---

## 0. 公開前に触る場所は2つだけ

### ① `index.html` 内の `LP_CONFIG`（ファイル下部・`<script>` の先頭）

```js
var LP_CONFIG = {
  bookingUrl: '{{予約URL}}',   // 予約ページのURL
  ibjMember: false,            // IBJ加盟契約が完了したら true
  showRatingScale: false,      // 「33項目×5段階」を正式運用していれば true
  showBonusReport: false       // 診断レポートを実際に渡せるなら true
};
```

### ② `{{ }}` の一括置換

GitHubのWeb編集画面で `{{サービス名}}` などを検索して置き換えます。
**`{{予約URL}}` は `LP_CONFIG.bookingUrl` と HTML内リンクの両方**にあります（両方置換してください）。

> 未置換の間、CTAは押しても外部遷移せず、ページ内の無料相談セクションへスクロールします（リンク切れ防止）。

---

## 1. 実装方式についての補足

| 項目 | 内容 |
|---|---|
| 実装 | 素のHTML＋CSS＋Vanilla JS（依存ライブラリなし） |
| 理由 | GitHub Pages 等の静的ホスティングにビルドなしで置けるため |
| React/TS/Tailwind版 | 必要であれば同じ構造・同じトークンで変換可能です |
| LP内フォーム | **設置していません** |

フォームを設置していないのは、仕様書26項の「個人情報を安全に管理できない実装の場合は、LP内フォームを作らず、外部予約ページへ遷移させる」に従ったためです。静的ホスティングでは送信内容をサーバー側で安全に受け取れません。
外部予約システム（Google フォーム、TimeRex、Spir、LINE公式アカウント等）を `{{予約URL}}` に設定してください。
LP内フォームが必要な場合は、フォーム専用のバックエンドを用意したうえで追加実装します。

---

## 2. 使用したコンポーネント一覧

| # | コンポーネント | セクションID | 背景色 |
|---|---|---|---|
| 1 | Header | `.hd` | #FFFFFF（固定） |
| 2 | MobileNav | `#mnav` | #FFFFFF |
| 3 | Hero | `.hero` | 白→ペールブルーの微斜めグラデーション |
| 4 | PainSection（共感） | `#empathy` | #FFFFFF |
| 5 | CauseSection（問題の原因） | `#cause` | #F4F6F8 |
| 6 | BeforeAfter | `#compare` | #EAF2F8 |
| 7 | MethodCycle（関係構築改善サイクル） | `#method` | #FFFFFF |
| 8 | ServiceCards（支援内容） | `#services` | #FCFBF7 |
| 9 | Results（実績） | `#results` | #122A43 |
| 10 | Testimonials（事例） | `#cases` | #FFFFFF |
| 11 | IBJSection ※加盟後のみ | `#ibj` | #EAF2F8 |
| 12 | TeamSection（代表・柴沼氏） | `#team` | #FCFBF7 |
| 13 | Pricing（料金） | `#pricing` | #FFFFFF |
| 14 | FitSection（向き不向き） | `#fit` | #F4F6F8 |
| 15 | ConsultationCTA（無料診断） | `#consult` | #122A43 |
| 16 | FAQ | `#faq` | #FFFFFF |
| 17 | FinalCTA | `#final` | #EAF2F8 |
| 18 | Footer | `.ft` | #122A43 |
| 19 | MobileStickyCTA | `.sticky` | #FFFFFF（767px以下） |

セクション順序は仕様書12項どおり：失敗体験（ヒーロー）→共感→問題の原因→解決の仕組み（Before/After＋メソッド）→支援内容→実績→事例→IBJ→代表と支援体制→料金→向いている人→無料診断→FAQ→最終CTA。

**文章データの編集場所**：事例カードは `<script>` 内の `CASES` 配列にまとまっています。5〜10件まで追加できます。`consent: true`（掲載許可あり）のカードのみ表示され、`comment` は空文字なら非表示になります。

---

## 3. 使用した画像と掲載位置の一覧

| 仕様書の記号 | ファイル | 掲載位置 | 加工内容 |
|---|---|---|---|
| 画像B | `images/hero-counselor.webp` | ヒーロー右カラム | **市松模様を除去して背景透過PNG化**→WebP（透過保持）。900×1086 |
| 画像G | `images/diagnosis-review.webp` | メソッド：診断・振り返りの補足 | WebP変換のみ |
| 画像E | `images/method-explain.webp` | メソッド：設計の補足 | WebP変換のみ |
| 画像F | `images/feedback-tablet.webp` | 支援内容セクション導入部 | WebP変換のみ |
| 画像H | `images/consultation.webp` | 無料診断セクション右側 | WebP変換のみ |
| 画像D | `images/counselor-portrait.webp` | 代表カウンセラー紹介（左） | **手書きコピー部分を除外し、女性部分のみトリミング** |
| 画像A | `images/counselor-walking.webp` | 最終CTA横 | WebP変換のみ |
| ― | `images/ogp.jpg` | OGP画像（1200×630） | 新規作成。左＝コピー／右＝代表画像 |

- 画像C（握手・会議・コーヒーのコラージュ）は**受領していないため未使用**です。必要なら各場面を個別に切り出してご送付ください。
- 画像内に主要コピーを焼き込んでいる箇所はありません（OGP画像のみ、仕様書32項の指定どおりコピーを配置）。
- 人物の顔・年齢・髪型・体形は一切変更していません。生成画像による補完も行っていません。

---

## 4. 未確定のプレースホルダー一覧（全33件）

### サービス・URL
- `{{サービス名}}` … ヘッダー／`<title>`／OGP／フッター
- `{{予約URL}}` … 全CTA（9箇所）＋ `LP_CONFIG.bookingUrl`
- `{{公開URL}}` … canonical／og:url

### 代表カウンセラー
- `{{代表者氏名}}` `{{代表者経歴}}` `{{代表者資格}}` `{{代表者メッセージ}}`

### プログラム設計責任者
- `{{柴沼氏正式氏名}}` `{{柴沼氏正式肩書}}` `{{柴沼氏経歴}}` `{{柴沼氏写真}}`

### 実績
- `{{実績対象期間}}` `{{交際開始の定義}}`

### 料金・支援範囲
- `{{月あたりの面談回数}}` `{{相談可能範囲}}` `{{返信時間}}` `{{回数または条件}}` `{{実施方法}}` `{{追加料金条件}}`

### FAQ
- `{{正式な面談頻度}}` `{{対面対応エリア}}` `{{IBJ加盟後の正式回答}}`※加盟後のみ

### IBJ（加盟後のみ）
- `{{IBJ公式データ・出典・調査時点}}`

### 会社情報・フッター
- `{{運営会社名}}` `{{代表者名}}` `{{所在地}}` `{{電話番号}}` `{{メールアドレス}}` `{{営業時間}}` `{{定休日}}`
- `{{運営会社URL}}` `{{プライバシーポリシーURL}}` `{{特定商取引法URL}}` `{{利用規約URL}}`

プレースホルダーは黄色の破線ボックスで表示されるため、置換漏れが目視で分かります。

---

## 5. 公開前に確認すべき事実一覧

| # | 確認事項 | 現状のLPでの扱い |
|---|---|---|
| 1 | IBJ加盟契約は完了しているか | `ibjMember: false`。未完了として全非表示 |
| 2 | IBJロゴの使用許諾を得ているか | ロゴは一切使用していません |
| 3 | 「24名／13名」の対象期間 | `{{実績対象期間}}` 未入力 |
| 4 | 「交際開始」の定義 | `{{交際開始の定義}}` 未入力 |
| 5 | 24名／13名は誰の実績か | 「過去の恋愛・婚活支援」「IBJ会員限定ではない」「成婚実績ではない」と3箇所に明記済み |
| 6 | 事例3件の本人掲載許可 | `consent: true` で仮設定。**許可未取得なら `false` にしてください** |
| 7 | 事例の年齢・職業の事実確認 | 仕様書記載内容をそのまま使用。要確認 |
| 8 | 代表者の経歴・資格 | 未入力（捏造していません） |
| 9 | 柴沼氏の正式氏名・肩書・経歴 | 未入力。リクルート勤務歴等を載せる場合は所属期間・役割の事実確認が必要 |
| 10 | 料金（165,000／28,000／275,000円）の最新値 | 仕様書の値をそのまま記載 |
| 11 | 成婚料の発生条件・解約条件 | `{{追加料金条件}}` 未入力。特商法表記側にも記載が必要 |
| 12 | 「33項目×5段階」の運用実態 | `showRatingScale: false` で非表示 |
| 13 | 診断レポートを実際に渡せるか | `showBonusReport: false` で非表示 |
| 14 | 対面対応エリアの有無 | `{{対面対応エリア}}` 未入力 |
| 15 | プライバシーポリシー・特商法ページの存在 | フッターリンクのURL未入力 |
| 16 | 予約システムの選定 | `{{予約URL}}` 未入力 |

---

## 6. スマートフォン表示の確認結果

実ブラウザ（Chromium）で 375×760 / 820×1100 / 1280×900 を実測しています。

| 項目 | 結果 |
|---|---|
| 375px幅での横スクロール | **発生なし**（`document.scrollWidth = 375`） |
| 820px（タブレット） | 発生なし。ヘッダーはロゴ＋CTA＋ハンバーガー |
| 1280px（デスクトップ） | 発生なし。ヘッダーはロゴ＋ナビ＋CTA |
| 2カラム→1カラム | ヒーロー／Before・After／メソッド／料金／代表／無料診断／最終CTA すべて縦積みに切替 |
| Before・After | 上下配置に変更（Beforeが上） |
| 実績数値 | 縦積みに変更 |
| 料金表 | 縦積み。**横スクロールなし** |
| CTA | 横幅ほぼ100%。固定CTAは高さ72px・ボタン52px・左右16pxマージン |
| 固定CTAによる本文隠れ | `body` に80pxの下余白を確保して回避 |
| 事例カード | 1枚ずつのスナップ横スクロール（自動再生なし）＋左右ボタン |
| 最小文字サイズ | 本文16px／注釈12.5px以上。10px以下の文字なし |

ブレークポイント：モバイル ～767px／タブレット 768～1023px／デスクトップ 1024px～

---

## 7. アクセシビリティ確認結果（WCAG 2.1 AA 相当）

### 満たしている項目
- H1はページ内に1つのみ。見出し階層は h1→h2→h3 の順で飛びなし
- 全画像に内容を説明する `alt` を設定（例：「ネイビーのスーツを着た女性代表カウンセラー」「男性相談者にタブレットを使って説明する女性カウンセラー」）
- 装飾用SVG・記号にはすべて `aria-hidden="true"`
- FAQは `<button>` ＋ `aria-expanded` / `aria-controls` / `role="region"`。**Tab・Enter・Spaceで開閉可能**、初期状態はすべて閉
- ハンバーガーは `aria-expanded` ＋ **Escapeキーで閉じる**
- フォーカスリングを3pxで明示（濃紺セクション上ではゴールド）
- 「本文へスキップ」リンクをフォーカス時に可視化
- 色だけで情報を区別していない（向き不向きは見出し文言＋アイコン形状で区別）
- CTAは `<a>`、開閉ボタンは `<button>` とHTML要素を正しく使い分け
- `prefers-reduced-motion: reduce` に対応（アニメーションとスムーススクロールを無効化）
- 事例カルーセルは自動再生なし、`tabindex="0"` でキーボードスクロール可能

### コントラスト実測（背景色ごと）
| 前景／背景 | 比 | 判定 |
|---|---|---|
| 本文 #33404D ／ #FFFFFF | 9.6:1 | AAA |
| 補足 #66727E ／ #FFFFFF | 4.9:1 | AA |
| 補足 #66727E ／ #F4F6F8 | 4.6:1 | AA |
| CTA文字 #122A43 ／ ゴールド #D5A62F | 6.3:1 | AA |
| ゴールド #D5A62F ／ 濃紺 #122A43 | 6.3:1 | AA |
| 白文字 ／ 濃紺 #122A43 | 16.8:1 | AAA |
| フッター #C3D2DF ／ #122A43 | 9.3:1 | AAA |

### 注意点（1点）
仕様書7項が指定する **CTAホバー時（背景 #B98A18 ＋ 白文字）は 3.1:1** で、通常サイズ文字のAA基準（4.5:1）を下回ります。
仕様を優先して実装していますが、通常時は 6.3:1 を確保しています。厳密にAAを通す場合は、ホバー時も文字を `#122A43` のままにするのが最も簡単な解決です。

なお、補足色 #66727E はペールブルー #EAF2F8 上では 4.35:1 と基準を下回るため、ペールブルー背景の注釈は本文色 #33404D または白カード内に配置しています。

---

## 8. 表示速度上の注意点

### 現状
| 項目 | 値 |
|---|---|
| `index.html` | 約 82KB（CSS・JS込み・gzip前） |
| 画像合計 | 約 218KB（OGP除く） |
| ヒーロー画像 | 80KB（WebP・透過） |
| 外部リクエスト | Google Fonts のみ |
| JavaScript | 依存ライブラリ0。約6KB相当 |

### 実施済みの対策
- 全画像をWebP化。`width` / `height` を明示して**レイアウトシフトを防止**
- ヒーロー画像のみ `fetchpriority="high"`、他はすべて `loading="lazy"`
- フォントは `display=swap` ＋ `preconnect`。ウェイトは Noto Sans JP 4種・Inter 3種に限定
- 自動再生動画・カルーセル・カウントアップなし
- アニメーションは `opacity` / `transform` のみ（レイアウト再計算なし）

### 公開時に確認したいこと
1. **フォントが最大のブロック要因**です。LCPが2.5秒を超える場合は、Noto Sans JPのウェイトを400・700・800の3種に減らすか、フォントファイルをセルフホストしてください。
2. ヒーロー画像は900px幅で書き出しています。モバイルでの実表示は約330pxなので、さらに詰めたい場合は `srcset` で440px版を追加すると効果的です。
3. GitHub Pagesはgzip配信されるため、HTMLの実転送量は約15KB程度になります。
4. 目標値：LCP 2.5秒以内／CLS 0.1未満／INP 200ms以内。CLSは寸法指定済みのため、ほぼ0で通る見込みです。

---

## 9. IBJ加盟前に非表示とすべき箇所

`LP_CONFIG.ibjMember = false` の間、以下が**自動で非表示**になります（`data-ibj` 属性で制御）。

1. **ヒーローの上部ラベル**「IBJ正規加盟店 × 関係構築コーチング」
2. **IBJセクション全体**（`#ibj`）
3. **FAQ Q6**「IBJの会員を紹介してもらえますか？」
   - Q番号は自動で振り直されるため、番号が飛ぶことはありません

加盟契約が完了したら `ibjMember: true` に変更し、あわせて次を実施してください。

- `{{IBJ加盟後の正式回答}}` を記入
- IBJの会員数・成婚数等を載せる場合は `{{IBJ公式データ・出典・調査時点}}` に**出典と調査時点**を明記
- IBJロゴは**公式提供素材のみ**を使用（自作・改変・色変更は不可）
- 自社実績（24名／13名）とIBJ全体の実績を並記しない

---

## 10. 公開前最終チェックリスト

### 表示制御
- [ ] `LP_CONFIG.bookingUrl` に予約URLを設定した
- [ ] HTML内の `{{予約URL}}`（9箇所）も置換した
- [ ] `ibjMember` を実態に合わせた
- [ ] `showRatingScale` を実態に合わせた
- [ ] `showBonusReport` を実態に合わせた

### 文言・事実
- [ ] `{{ }}` の黄色い破線ボックスがページ上に1つも残っていない
- [ ] 実績の対象期間・交際開始の定義を記入した
- [ ] 24名／13名を成婚実績・IBJ実績として表示していない
- [ ] 事例3件の本人掲載許可を取得した（未取得なら `consent: false`）
- [ ] 代表者・柴沼氏の経歴に未確認情報が含まれていない
- [ ] 料金・成婚料の発生条件が最新である

### 法務
- [ ] プライバシーポリシーページを公開しリンクした
- [ ] 特定商取引法に基づく表記を公開しリンクした
- [ ] 利用規約・運営会社ページをリンクした
- [ ] 「必ず」「絶対」「成婚率○％」等の断定表現がない（現状なし）
- [ ] 交際・成婚を保証しない旨が実績欄とフッターに記載されている（記載済み）

### 技術
- [ ] スマホ実機で横スクロールが出ない
- [ ] 全CTAが予約ページに遷移する
- [ ] UTMパラメータが予約ページまで引き継がれる
- [ ] FAQがキーボードで開閉できる
- [ ] OGP画像がSNSのカードプレビューに正しく出る（デバッガで確認）
- [ ] `{{公開URL}}` を canonical に設定した
- [ ] GA4 / GTM を設置し、下記イベントが飛ぶことを確認した

---

## 11. 計測イベント

CTAには `data-cta` 属性が付いており、クリック時に `window.dataLayer` へpushされます。`gtag` があれば同時に送信します。

| イベント名 | 発火場所 |
|---|---|
| `cta_header` | ヘッダー／モバイルメニュー |
| `cta_hero` | ファーストビュー |
| `cta_after_problem` | 問題の原因セクション末尾 |
| `cta_after_method` | メソッドセクション末尾 |
| `cta_pricing` | 料金セクション |
| `cta_consult` | 無料診断セクション（仕様書の一覧にない追加分） |
| `cta_final` | 最終CTA |
| `cta_mobile_sticky` | モバイル固定CTA |
| `cta_footer` | フッターのお問い合わせ |
| `faq_open` | FAQを開いたとき（質問文をパラメータ付与） |

`form_start` / `form_submit` はLP内フォーム未設置のため未実装です。外部予約ページ側で計測してください。

### 流入元の判別
`utm_source` `utm_medium` `utm_campaign` `utm_term` `utm_content` `gclid` `fbclid` `twclid` `ttclid` `yclid` を予約URLへ自動で引き継ぎます。
さらに `lp_cta=（CTA名）` を付与するため、**どのCTAから予約されたか**が予約ページ側で判別できます。

広告出稿時のURL例：
```
https://（公開URL）/?utm_source=x&utm_medium=cpc&utm_campaign=konkatsu_2026aug
https://（公開URL）/?utm_source=instagram&utm_medium=cpc&utm_campaign=konkatsu_2026aug
```

---

## 12. GitHub Pages での公開手順

1. GitHubで新しいリポジトリを作成
2. `index.html` と `images/` フォルダをそのままアップロード（Add file → Upload files）
3. Settings → Pages → Source を `main` ブランチ / `/ (root)` に設定
4. 1〜2分後に `https://（ユーザー名）.github.io/（リポジトリ名）/` で公開されます
5. 公開URLを `{{公開URL}}` に記入して再アップロード
